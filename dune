(ocamllex
  (modules lexer))
(menhir
  (modules parser))
(executable
  (name pyretc)
  (promote (until-clean))
  (modules ast typed_ast
           lexer parser typer
           x86_64 compile pyretc
           pretty_printer pretty_type_printer )
  (flags ()))
